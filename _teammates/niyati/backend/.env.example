# FastAPI email OTP (never put the Gmail app password in source)
SMTP_EMAIL=
SMTP_APP_PASSWORD=
# Optional aliases used by older Amongus notes:
# EMAIL=
# APP_PASSWORD=
# Local demo without SMTP — OTP is printed and returned in JSON
AUTH_DEV_MODE=false
